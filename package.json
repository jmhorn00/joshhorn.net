{
  "name": "core-static",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.js",
    "build": "vite build --config vite.config.js",
    "build:watch": "vite build --watch",
    "preview": "vite preview --config vite.config.js --host 127.0.0.1 --port 4173"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
